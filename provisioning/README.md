Device provisioning — install apps while you still have internet.

This is separate from svalbard (the archive builder). These are
recommendations and scripts for setting up devices to USE the drives.

macOS:
  brew bundle --file provisioning/Brewfile

See also:
  android.md — recommended Android apps
  ios.md     — recommended iOS apps
